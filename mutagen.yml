sync:
  app:
    mode: "two-way-resolved"
    alpha: "./"
    beta: "date-suggester:/home/vagrant/app"
    ignore:
      vcs: true
      paths:
        - "/node_modules"
        - "/log"
        - "/tmp"
